fastapi==0.115.5
uvicorn[standard]==0.32.1
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
pydantic==2.10.3
# live categorizer path (AnthropicLLMClient); tests mock it, so it is optional
# for the gate but required to run the real LLM fallback. Pinned to the version
# the code targets: messages.create(..., output_config=...) structured output.
anthropic==0.115.1
# test deps
pytest==8.3.4
httpx==0.28.1
